import { Canvas, ArgTypes, Meta, Subtitle, Title } from '@storybook/blocks'
import * as FullscreenMapStories from './FullscreenMap.stories'

<Meta of={FullscreenMapStories} />

<Title>FullscreenMap</Title>
<Subtitle>Immersive map experience with synchronized sidebar, inspector panel, and mobile carousel for full exploration</Subtitle>

## Usage

```tsx
import { FullscreenMap } from "@ainativekit/ui";
```

<Canvas of={FullscreenMapStories.Base} />

## Reference

<ArgTypes of={FullscreenMapStories.Base} />

## Examples

### Loading

Show skeleton map while data is loading with `loading={true}`.

<Canvas of={FullscreenMapStories.Loading} />

### Error

Display an error message when content fails to load with `error={true}`. Add retry functionality via `onErrorRetry`.

<Canvas of={FullscreenMapStories.Error} />

### Empty

Show an empty state when there are no locations.

<Canvas of={FullscreenMapStories.Empty} />

### States overview

All map states at a glance.

<Canvas of={FullscreenMapStories.States} sourceState="none" />
